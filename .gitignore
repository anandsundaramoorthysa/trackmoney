# dependencies
node_modules/
.pnp
.pnp.*

# next
.next/
out/
build/
next-env.d.ts

# env — never commit real keys, test-mode included
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# test artifacts
test-results/
playwright-report/
.playwright-mcp/

# Kept on disk, deliberately out of the public repo: the internal planning
# notes and the test suite. Both stay in the working tree, so the suites still
# run locally. README.md and docs/ are public and stay tracked.
PLAN.md
tests/
# Meaningless without tests/, so it travels with them.
tsconfig.tests.json

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-error.log*
.idea/
.vscode/
coverage/

.vercel
.env*

# Kept locally, not published. The pitch and the internal guides are written for
# one audience and one week; the README is what a stranger arrives at.
PITCH.md
docs/
